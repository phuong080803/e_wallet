class ExternalPaymentConfig {
  // Placeholder wallet ID to receive funds for external payouts (demo mode)
  // Create this wallet in DB for your admin/service account, or change the ID below.
  static const String payoutWalletId = '9999999999';
}
